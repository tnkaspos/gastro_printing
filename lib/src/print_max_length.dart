class PrintMaxLength {
  int quantity;
  int itemID;
  int single;
  int total;
  int lineA;
  int lineB;

  PrintMaxLength(this.quantity, this.itemID, this.single, this.total, this.lineA, this.lineB);
}
